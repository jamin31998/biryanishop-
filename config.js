/* ====================================================
   CONFIG.JS
   General website configuration.
   Edit this file to change global site-level settings.
   ==================================================== */

const CONFIG = {
  // Site metadata (used for <title> and meta tags via script.js)
  siteTitle: "YOUR SHOP NAME | Biryani & Litti Outlet",
  siteDescription: "YOUR TAGLINE — Order fresh Beef Biryani and Litti. Pre-book instantly on WhatsApp.",

  // Currency symbol used across menu prices
  currencySymbol: "₹",

  // Smooth scroll offset (px) to account for the sticky header height
  scrollOffset: 72,

  // Copyright name shown in the footer (kept separate so it can differ from shop name if needed)
  copyrightName: "YOUR SHOP NAME",
};
