/* ====================================================
   FEATURES.JS
   Website ON / OFF settings.
   Toggle any section or button by switching true / false.
   script.js reads these values — do not edit HTML to
   show or hide a feature.
   ==================================================== */

const FEATURES = {
  gallery: true,
  whatsappButton: true,
  callButton: true,
  floatingButtons: true,
  preBook: true,
  statusCard: true,
  onlinePayment: false, // Keep OFF until a real payment method is ready
};
